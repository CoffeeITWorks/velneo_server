La licencia de velneo no permite la distribución del software ni en su versión gratuita.
Por esto no se puede incluir en este rol.

Cree la carpeta files en la misma ubicación que su playbook.yml y guarde los archivos:

velneo.tar.gz
velneo-vmodapache.tar.gz

Coloque una nota sobre la versión que se incluye.
